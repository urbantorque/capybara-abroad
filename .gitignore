dist/
node_modules/
.playwright-cli/

# QA captures — none of them, at any depth. `qa/*.png` only ever matched the
# TOP level, so every PNG written into a subdirectory was tracked by default,
# which is how qa/pass/ came to hold 29 MB of a 33 MB repository.
#
# THE HERO SHOTS ARE NO LONGER TRACKED EITHER, and that was a deliberate call
# rather than an oversight. They were 26 curated frames, one per chapter, added
# in the root commit and never touched again, referenced by nothing — 87 % of
# the weight of a repository that is about to be published. They still exist on
# the author's disk; they are simply not the repository's problem. See
# REVIEW-2026-08-31.md.
qa/**/*.png

# Scratch that reached the root and dodged `qa/**/*.png` by not being in qa/.
# Same call as the hero shots above: a 120 KB screenshot from 24 Aug and a
# P6 duplicate-finder named `.tmp` are on the author's disk and are not the
# repository's problem. Nothing in the tree referenced either.
dp-top.png
*.tmp.cjs

# A D6b contact sheet: one <style> and nineteen <figure>s pointing at
# `qa/d6b-*.png`, which the rule above already excludes — so in a clean
# checkout it renders nineteen broken images. Same call as `dp-top.png`: a
# scratch viewer on the author's disk, referenced by nothing in the tree.
qa/sheet.html

# The soak's staged copies of the three probes it runs (qa/soak.mjs rewrites
# their port and dist/ path per run). The history it appends to,
# qa/soak-history.jsonl, IS tracked: one line per run is the point of it.
qa/.soak/

# Local listening takes; the capture instrument and evidence protocol are tracked.
qa/homecoming-audition-*.webm
